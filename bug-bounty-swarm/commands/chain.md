# /chain Command

## Purpose
Analyze finding combinations for exploit chains.

## Usage
`/chain target=<domain_or_url>`

## Example chains
- IDOR + privilege escalation
- Open redirect + OAuth token leakage
- SSRF + cloud metadata exposure + credential reuse
